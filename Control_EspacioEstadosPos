function giro=EspacioEstadoPosicion(Pos,Vel,K,REF)

        EstadoAct=[Pos;Vel];
        giro=(K(1)*REF-K*EstadoAct);
        if giro>2*pi
            giro=2*pi;
        elseif giro<-2*pi
            giro=-2*pi;    
        end
end
